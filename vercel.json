{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter ./packages/sdk build && pnpm --filter ./packages/shared build && pnpm --filter ./packages/react build && pnpm --filter ./admin build",
  "outputDirectory": "admin/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
